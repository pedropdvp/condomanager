package com.condomanager.model.enums;

/**
 * Perfis (roles) do sistema, conforme matriz de permissoes do planeamento.
 */
public enum NomePerfil {
    ADMIN_SISTEMA,
    GESTOR,
    FUNCIONARIO,
    ADMIN_CONDOMINIO,
    CONDOMINO
}
